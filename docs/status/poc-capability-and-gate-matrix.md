# POC capability and gate-status matrix

Status snapshot: 2026-08-23 working tree. Neutral comparison, uncapped all-route overview, shared selection, tolerant gap projection, governed airport-name enrichment, recorded route-element airway labels, and a computed direct great-circle alternate (ADR-0003) are implemented and focused-tested locally. The earlier donor-subpath synthesis capability was removed. Azure remains unauthorized and unexecuted.

This matrix records the current status of every `AC-POC-*` acceptance criterion
(binding design Section 0.6 and implementation plan Section 8.3) and of the
`PG-00` through `PG-04` gates. It is a documentation artifact, not evidence: a
status moves to "evidenced" only when a retained record with exact subject,
procedure, measurements, and artifact hashes exists (see
[Testing and evidence contract](../testing/evidence-and-validation.md)).

Classification terms:

- **Implemented and evidenced:** code exists and a retained record documents the result.
- **Partially implemented / partially evidenced:** core behavior exists, but a mandated part is missing or its evidence is not retained.
- **Designed/planned only:** documented contract or operator procedure; no execution.
- **Deferred / blocked on authorization:** deliberately postponed, or requires user authorization (for example any Azure write).

Record naming: every record remains bound to the subject and artifact hashes it names. `loopback-lane-local-e965c728fe45.json` is the current 23/23 neutral-contract fixture lane; older Rank-era records are immutable historical evidence and do not prove the revised overview/comparison/name contract.

## Acceptance criteria

| Criterion | Status | What exists today (with citations) | Gap / next action |
|---|---|---|---|
| `AC-POC-LOCAL-01` — authoritative secretless-CI OCI digest passes the complete loopback-only five-family real-data container gate before any Azure write | Implemented and evidenced (2026-08-21 authorized six-check container run) | Authoritative CI subject: `docs/evidence/oci-digest-bundle-828a70968420.json` (PR #46 merge commit `828a7096`, branch head `0c2c689`, digest `sha256:8cc7a862…`, `ci-build` stamped, all image assertions pass, CI Trivy 0 vulnerabilities — `docs/security/trivy-scan-ci-828a7096.json`). The authorized six-check container live run passed 6/6 on that exact digest (`docs/evidence/container-live-lane-0c2c689696f2.json`): exact subject, liveness, five-family acquisition, browse exact-once (92 flights), synthesis honest aggregation (5 targets, all `unavailable` — a legitimate honest live outcome), secret exclusion. The `PG-03` manifest (`docs/evidence/oci-subject-local.json`) records `gateResult: pass` with all four checks pass. The superseded 2026-08-15 five-check record remains archived byte-for-byte (`docs/evidence/archived/container-live-lane-afe29166ac21.json`) | The Azure leg remains deferred behind authorization; no cloud write is authorized by this gate pass |
| `AC-POC-BROWSE-01` — server/client cursor traversal returns every flight exactly once and the ready UI shows every safe flight/resolved component without a 10-route cap | Implemented and focused-tested; current loopback exact-once check evidenced | Server browse and client `fetchRouteOverview` enforce generation on every page, reject duplicate `flightId`, generation changes, and non-progressing pages. Focused client tests pass 4/4; interaction tests cover 12 list rows and 12 map paths plus filter parity. Current loopback record passes `LANE-BROWSE-EXACT-ONCE` | Revised real-data visual-density/UAT record pending; older browse records prove only their named subjects |
| `AC-POC-COMPARE-01` — selected-first immutable source order, neutral complete/incomplete groups, descriptive distance, explicit gaps, and no public preference fields | Implemented and focused-tested; current loopback check evidenced | Public DTOs omit `rank`, `rankDistanceNm`, `rankLabel`, and `operationalProxy`; route-safety/runtime/API-contract suites pass 30/30. Current loopback record passes `LANE-NEUTRAL-ORDER`. UI copy and groups are neutral | Historical Rank-era records remain immutable older-contract evidence; revised live UAT pending |
| `AC-POC-LIVE-01` — local and Azure POC flows acquire and validate real five-family data; cold startup fails on any unusable family; refresh retains only a still-usable complete generation; no synthetic runtime/demo fallback | Implemented and evidenced locally; Azure leg un-evidenced | Tiered plan §6.2 freshness: `packages/upstream-caas/src/freshness.ts` (live 5 min fresh / 30 min unusable; reference 24 h / 7 d; inclusive boundaries; fail closed on unusable). GenerationStore retains active + previous generation and swaps atomically, keeping the previous only while servable (`apps/api/src/server.ts:429-430,465-485`); readiness fails closed with `GENERATION_STALE` (`server.ts:498-500`); refresh token fail-closed (`server.ts:1014-1019`). Loopback lane passes `LANE-STARTUP-READY`, `LANE-FIVE-FAMILY-ACQUISITION`, `LANE-GENERATION-TIERS`, `LANE-REFRESH-RETAINS`, `LANE-REFRESH-AUTH`, `LANE-REFRESH-BOUND`, `LANE-FAIL-CLOSED-STARTUP`, `LANE-RESTART-REACQUISITION`. Authorized live run passes `LIVE-LIVENESS`, `LIVE-FIVE-FAMILY-ACQUISITION`, `LIVE-BROWSE-EXACT-ONCE`, `LIVE-REFRESH-AUTH`, `LIVE-SECRET-EXCLUDED` (`docs/evidence/live-lane-116a84d608f3.json`) | Azure-leg acquisition remains un-evidenced and unauthorized |
| `AC-POC-DATA-01` — Airways hidden; exact ambiguity/gap preservation; governed endpoint-name enrichment | Implemented and focused-tested | Airways exclusion and exact gap behavior remain pinned. Tolerant overview projection retains resolvable interior segments around endpoint gaps. Airport labels use the pinned 10,444-record OurAirports exact-ICAO bundle with deterministic generation, checksum/count/license manifest, fallback, and no fuzzy/runtime lookup; 4/4 governance tests pass | New bundle-specific gate record is not claimed; source is community-maintained, not official ICAO |
| `AC-POC-MAP-01` — attribution, accepted IP/tile disclosure, origin-only Referer, caching, no-prefetch, configurable provider, non-map tile-failure behavior | Implemented and evidenced — owner-authorized OSM tiles (2026-08-15) | Dependency-free Web Mercator tile layer (`apps/web/src/TileMap.tsx`) renders `https://tile.openstreetmap.org/{z}/{x}/{y}.png`; tile URLs carry z/x/y only, requests are `no-referrer`, attribution `© OpenStreetMap contributors` shown, CSP `img-src` allows only that host (`apps/api/src/server.ts:58`, pinned by `tests/api-contract.test.ts`), zoom bounded 1-19 with ≤64 tiles per frame, and tile failure/toggle falls back to the schematic base map with Route Data still usable. Tile contract pinned by `tests/e2e/map-tiles.test.tsx`; `LANE-SECURITY-HEADERS` passes; web import boundary pinned by `LINT-WEB-BOUNDARY`; forced-colors/reduced-motion overrides extend to the tile controls | Production-level provider terms/privacy/quota review remains open (production register); the legacy "configurable provider" option stays non-binding archived material |
| `AC-POC-SEC-01` — browser never receives the CAAS key/raw object; strict application allow-list tests and the accepted no-firewall residual are recorded | Implemented and evidenced | Server-only key confirmed in the authorized live run (`LIVE-SECRET-EXCLUDED` pass). Security lane `docs/evidence/security-local-116a84d608f3.json`: 8/8 pass, including `SEC-PACKAGE-AUDIT` on the retained authorized networked audit (0 advisories, `docs/security/dependency-audit-local.json`). CI gates at `116a84d`: Semgrep (0 blocking findings, supply-chain policies in `pnpm-workspace.yaml`), gitleaks secret scan, dependency audit. No-firewall residual and allow-list contracts documented (README, safety, operations, `deploy/poc-policy.yaml`) | None blocking; re-audit at the next dependency-bump cycle |
| `AC-POC-REL-01` — unchanged-digest direct deployment, first-deploy ingress-disabled abort/cleanup, later prior-revision plus complete app-scoped configuration rollback, smoke, and current-data reacquisition | Designed and procedurally defined; not evidenced — blocked on authorization | Operator procedures: `docs/operations/azure-release-path.md`, `azure-preflight-and-bootstrap.md`, `azure-deployment-procedure.md`, `azure-abort-and-rollback-drills.md`, `azure-post-demo-verification.md`; topology contract `docs/architecture/azure-poc-topology.md`. No Azure deployment, drill, or smoke execution is evidenced; all cloud writes are unauthorized | Requires user authorization and the completed `PG-03` evidence before any cloud write |
| `AC-POC-DOC-01` — architecture, algorithms, tooling, build/deploy, limitations, AI use, lessons, requested feedback, and future roadmaps documented | Implemented and evidenced | README, documentation index, POC boundary, data-use, operations (local + Azure release path), security, testing (incl. accessibility/UAT), and ADR documents; historical archive; this matrix | The 30-minute walkthrough and UAT remain pending execution (`docs/testing/uat-walkthrough.md`, `docs/operations/uat-and-timed-walkthrough.md`) |

## Feature capability rows (non-criterion)

| Capability | Status | What exists today (with citations) | Gap / next action |
|---|---|---|---|
| Computed direct alternate — the densified direct great-circle path between resolved departure and destination airports ([ADR-0003](../adr/0003-airway-labels-and-direct-alternate.md)) | Implemented and focused-tested locally | `POST /api/v1/routes/alternate` (`{flightId}` → geometry, full-precision distance, `direct-great-circle` label, safety copy) in `apps/api/src/server.ts`, backed by `packages/route-engine/src/alternate.ts`; engine/api/e2e/a11y/responsive suites and adversarial negatives | Revised human UAT of the alternate remains pending under the UAT kit |

## Gate status

| Gate | Status | Evidence present | Blocker / next action |
|---|---|---|---|
| `PG-00` | Partially closed; the two design §0.7 items remain framed as user determinations | Secret-free discovery manifest (`docs/evidence/pg-00-live-api-discovery.json`); independent exact-hash reviews of design `1.2-rc4` and plan `1.4-rc4` report no unresolved P0/P1; design `1.2-rc5` (no-tile SVG map boundary) and plan `1.4-rc5` (command-matrix tree status, supply-chain scope, `PLAN-R-18` eligibility API) await independent exact-hash re-review; documents committed and merged on `master` (`0dd2542` merge of the documentation workstream; final integration `1c838bd`); the `rc5` amendments live on PR #38 | (1) The explicitly authorized exact-hash record — the documents are now committed and merged, but whether that satisfies the §0.7 requirement is the user's determination, not a documentation claim; (2) a later explicit implementation request — open. No review or plan edit grants either authority |
| `PG-01` | Implemented locally and in CI | `lint` is a real check: `scripts/validation/lint-import-boundaries.mjs` (`LINT-WEB-BOUNDARY`, `LINT-SCRIPTS`, `LINT-ROOT-VALIDATION`), 3/3 pass at `docs/evidence/lint-local-116a84d608f3.json`; `typecheck` passes (5 projects); CI executes on PRs and is green at `116a84d` — `ci-secretless-validation` (evidence validation, Semgrep 0 blocking, gitleaks secret scan, dependency audit), `oci-subject-build` (image assertions + Trivy), `poc-pr-static` | `format:check` remains absent; CI has run for pull requests only (no push to `master` yet) |
| `PG-02` | Substantially implemented; revised focused validation passes; manual revised UAT pending | Current focused runs: accessibility 17/17, overview/interaction/keyboard 32/32 (4 client traversal + 28 UI), API 29/29 including 4 airport governance tests, neutral route-safety/runtime/API-contract 30/30, responsive 8/8; current loopback 23/23. Older retained lane/browser records remain subject-bound | Retain a new broad subject-bound gate record and execute revised human UAT/assistive-technology procedure |
| `PG-03` | Pass (2026-08-21 authorized six-check container run on the CI-built synthesis subject; previously honestly re-blocked, and passed 2026-08-15 on the pre-synthesis subject) | Gate manifest `docs/evidence/oci-subject-local.json` records `gateResult: pass` — `PG03-EXACT-DIGEST` pass, `PG03-LOOPBACK-REAL-DATA` pass, `PG03-SECRETLESS` pass, `PG03-RESOURCE-SHAPE` pass — on the verified CI subject (digest `sha256:8cc7a862…`, CI bundle `docs/evidence/oci-digest-bundle-828a70968420.json`). The authorized run `docs/evidence/container-live-lane-0c2c689696f2.json` passed 6/6 on the exact digest: liveness, five-family acquisition, browse exact-once (92 flights), synthesis honest aggregation (5 targets, all `unavailable`), secret exclusion; CI Trivy 0 vulnerabilities (`docs/security/trivy-scan-ci-828a7096.json`). The superseded 2026-08-15 five-check record remains archived byte-for-byte (`docs/evidence/archived/container-live-lane-afe29166ac21.json`) | The gate pass removes the PG-03 blocker but grants no cloud authority: every Azure write still requires the §0.5 authorization sequence |
| `PG-04` | Deferred; blocked on authorization | None executed. Operator procedures are ready (Azure release path, preflight, deployment, abort/rollback drills, post-demo verification) | Requires user authorization and the completed `PG-03` evidence before any cloud write |

## Cross-workstream handoffs

- `AC-POC-COMPARE-01` neutral alignment: **implemented and focused-tested** — design, plan, contracts, API/UI, tests, loopback lane, and current walkthrough prohibit preference fields and use stable neutral ordering.
- CI execution evidence and the authoritative OCI subject (`PG-01`/`PG-03`): CI green on PR #46 at branch head `0c2c689`; the CI-built synthesis digest bundle is retained (`docs/evidence/oci-digest-bundle-828a70968420.json`) and the authorized six-check container live run on that exact digest passed 6/6 (`docs/evidence/container-live-lane-0c2c689696f2.json`). Older CI bundles (`docs/evidence/oci-digest-bundle-e456dd0cd791.json`, `oci-digest-bundle-0962c7fedb67.json`) remain historical subject-bound evidence.
- Azure deployment, preflight, drills, and post-demo verification (`PG-04`, `AC-POC-REL-01`): Azure workstream; all cloud writes remain unauthorized.
- Assistive-technology verification and UAT execution: accessibility/UAT workstream (deterministic portions proved; manual steps pending).
- Data-use authorization: owner decision is `AUTHORIZED` for the scope recorded in `docs/data-use/data-use-record.md` and `docs/data-use/data-use-authorization-gate-status.yaml`; it grants no broader redistribution authority.
- Production gate (`PG-PROD`-family prerequisites): production-boundary workstream (`docs/operations/production-*.md` registers remain open; production stays prohibited).

## Historical evidence boundary

Generated evidence is never rewritten to match a newer contract. Rank-era records prove only their named older subject and checks. Current neutral-comparison, all-route overview, shared-selection, tolerant-projection, and airport-bundle claims require current focused tests or a new subject-bound retained record. Documentation changes alone cannot reclassify a gate.

## Updating this matrix

Reclassify a row only with retained evidence (a gate manifest, an executed
command's retained record with artifact hashes, or a user-authorized decision).
Documentation wording does not change a status; evidence does. When a status
changes, update this file, the documentation index, and the affected gate's
section in the implementation plan together.
